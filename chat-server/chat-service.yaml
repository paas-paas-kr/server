apiVersion: v1
kind: Service

metadata:
  name: chat-service

spec:
  type: LoadBalancer
  selector:
    app: chat-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      # nodePort: 30000
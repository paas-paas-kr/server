apiVersion: v1
kind: Service

metadata:
  name: member-mysql-service

spec:
  type: ClusterIP
  selector:
    app: member-mysql-db
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306

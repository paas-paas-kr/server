apiVersion: v1
kind: Service

metadata:
  name: member-service

spec:
  type: LoadBalancer
  selector:
    app: member-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      # nodePort: 30001
